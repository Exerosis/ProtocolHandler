package me.exerosis.packet.wrappers.in;

import me.exerosis.packet.wrappers.PacketWrapper;

public class PacketWrapperInArmAnimation extends PacketWrapper {
    public PacketWrapperInArmAnimation(Object packet) {
        super(packet);
    }
}